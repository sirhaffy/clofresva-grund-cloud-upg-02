{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Storage": {
    "AccountName": "{% if storage_account %}{{ storage_account }}{% else %}{% endif %}",
    "BlobEndpoint": "{% if blob_endpoint %}{{ blob_endpoint }}{% else %}{% endif %}",
    "ContainerName": "{% if container_name %}{{ container_name }}{% else %}appdata{% endif %}"
  },
  "MongoDB": {
    "ConnectionString": "{% if mongodb_connection_string %}{{ mongodb_connection_string }}{% else %}{% endif %}",
    "DatabaseName": "cloudsoft",
    "CollectionName": "subscribers"
  },
  "SUBSCRIBER_REPOSITORY": "{% if mongodb_connection_string and mongodb_connection_string|length > 20 %}cosmos{% else %}inmemory{% endif %}",
  "FEATUREFLAGS": {
    "USEAZURESTORAGE": {% if storage_account and blob_endpoint %}true{% else %}false{% endif %}
  }
}